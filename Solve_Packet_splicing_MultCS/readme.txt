In order to solve the promblem of packet splicing during the transilation of TCP,We can do the folloing things:
1:we always send the fixed length packet.
2:we define our own protocol

The implimention is in the source code.
